# 개발 서버 (Jenkins develop 배포, AWS RDS dev)
# DB_URL / DB_USERNAME / DB_PASSWORD / JWT_SECRET / CORS_ALLOWED_ORIGINS 는 Jenkins Credentials 또는 AWS Parameter Store 로 주입
logging:
  level:
    com.ilog: info
